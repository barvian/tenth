import type { PageLoad } from './$types'

export const load: PageLoad = () => ({
	meta: {
		title: 'Request a charity',
		description: `Don't see a charity you'd like to support on Tenth? Request it for addition through our donation partner Change`
	}
})
